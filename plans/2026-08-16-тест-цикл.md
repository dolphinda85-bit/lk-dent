# План: тест рабочего цикла
Дата: 2026-08-16

## Цель

Проверить, что проектный процесс проходит полный путь: правила → план → выполнение → проверка → итог → ретро.

## Контекст

Онбординг почти завершён. Микрозадача должна создать в корне проекта файл `ПРИВЕТ.txt` с одной заданной строкой.

## Фазы

- [x] Создать `ПРИВЕТ.txt` с текстом «настройка проверена».
- [x] Проверить существование файла и точное содержимое.
- [x] Отметить план выполненным и заполнить итог.

## Критерии готовности

- Файл существует в корне проекта.
- В файле ровно одна строка: «настройка проверена».
- Все фазы отмечены `[x]`, итог заполнен.
- Создано короткое ретро.

## Риски

- Ошибка кодировки или лишняя строка. Проверить содержимое и количество строк командой чтения.

## Открытые вопросы

Нет.

## Итог (в конце)

- Реализовано целиком: да.
- Что сделано: создан `ПРИВЕТ.txt`; подтверждены существование, одна строка и точное содержимое.
- Что не сделано: отсутствует.
- Урок: при проверке одной строки в PowerShell нужно оборачивать `Get-Content` в массив, иначе `[0]` обращается к первому символу строки.
